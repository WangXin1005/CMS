# ============================================
# IP 限制规则（可选启用）
# 取消注释下方 include 到 default.conf 的 server 块中即可生效
# ============================================

# 管理后台路径 IP 限制示例：
# location /admin {
#     allow 你的办公IP;
#     deny all;
# }
